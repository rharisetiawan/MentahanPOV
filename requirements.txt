# Core
python-dotenv>=1.0.0
tenacity>=8.2.0
requests>=2.31.0
Pillow>=10.0.0  # placeholder watermark text render (core/watermark.py)

# Google APIs (Drive + YouTube)
google-api-python-client>=2.120.0
google-auth>=2.27.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# Gemini (google-generativeai is deprecated; google-genai is its replacement)
google-genai>=0.3.0

# TikTok (browser automation fallback)
playwright>=1.42.0

# Telegram bot front-end (telegram_bot.py)
# [job-queue] extra pulls in APScheduler, needed for the daily /status
# report (telegram_bot.py's daily_status_job via job_queue.run_daily).
python-telegram-bot[job-queue]>=21.0
tzdata  # zoneinfo needs this on images without an OS tz database (python:slim)

# Status dashboard (dashboard.py) + Telegram /status (core/health.py)
flask>=3.0.0

# Dev / lint (optional)
ruff>=0.4.0
